#!/usr/bin/env bash
if [[ $# -eq 0 ]];then 
  echo "USAGE: binfile [list of programs on \$PATH]"
  echo "       binfile nmap"
  echo "       binfile pwd cd ls vim help"
  exit 
fi 

command_path=$(which "$@" )
for cmd in $command_path; do
  echo -n "$cmd ----> "
  file -b "$cmd"
done

