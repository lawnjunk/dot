#!/usr/bin/env bash
if [[ $# -eq 0 ]];then 
  echo "USAGE: binstat [list of programs on \$PATH]"
  echo "       binstat nmap"
  echo "       binstat pwd cd ls vim help"
  exit 
fi 

command_path=$(which "$@" )
for cmd in $command_path; do
  echo ""
  stat -x "$cmd"
done
