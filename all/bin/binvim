#!/usr/bin/env bash

if [[ $# -eq 0 ]];then 
  echo "USAGE: binvim [list of programs on \$PATH]"
  echo "       binvim nmap"
  echo "       binvim pwd cd ls vim help"
  exit 
fi 

command_path=$(which "$@")
if [[ "$command_path" ]] && (( $? == 0 ));then
  vim $command_path
  exit
fi
